export const mutateMockTool = {
  name: "mutate_mock",
  description:
    "修改 Mock 规则。op=create 创建（可从请求/模板/手动），op=update 更新（支持整体替换/patch/operations），op=delete 删除，op=enable 启停规则，op=set_group 切换激活分组。"
};


export const queryMockTool = {
  name: "query_mock",
  description:
    "查询 Mock 规则。action=list 获取规则摘要（支持 name/group/method/url/enabled 过滤），action=detail 获取规则详情（diagnostic 视图可传 requestId），action=active_group 查看当前激活分组。"
};

